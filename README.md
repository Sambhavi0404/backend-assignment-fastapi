# Backend Assignment – FastAPI (Lyftr AI)

Implements:
- HMAC-secured POST /webhook
- GET /messages with pagination, filters, ordering
- GET /stats with analytics
- /metrics Prometheus endpoint
- JSON logs (one line per request)
- /health/live and /health/ready probes
- SQLite storage via Docker volume

## Run
